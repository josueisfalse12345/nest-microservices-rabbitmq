# This file is generated by Nx.
#
# Build the docker image with `npx nx docker-build number-action-monorepo`.
# Tip: Modify "docker-build" options in project.json to change docker build args.
#
# Run the container with `docker run -p 3000:3000 -t number-action-monorepo`.
FROM docker.io/node:lts-alpine

ENV HOST=0.0.0.0
ENV PORT=3000

WORKDIR /app

RUN addgroup --system number-action-monorepo && \
          adduser --system -G number-action-monorepo number-action-monorepo

COPY dist/apps/number-action-monorepo number-action-monorepo/
RUN chown -R number-action-monorepo:number-action-monorepo .

# You can remove this install step if you build with `--bundle` option.
# The bundled output will include external dependencies.
RUN npm --prefix number-action-monorepo --omit=dev -f install

CMD [ "node", "number-action-monorepo" ]
